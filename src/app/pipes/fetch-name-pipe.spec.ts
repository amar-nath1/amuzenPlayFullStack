import { FetchNamePipe } from './fetch-name-pipe';

describe('FetchNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchNamePipe();
    expect(pipe).toBeTruthy();
  });
});
